import 'package:flutter/material.dart';
import '../page/connected/wifi.dart';

class testone extends StatelessWidget {
  const testone({super.key});

  @override
  Widget build(BuildContext context) {
    // return Scaffold(body: UpgradeWidget(),);
    return const Wifi();
  }
}